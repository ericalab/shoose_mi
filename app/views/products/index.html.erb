<div class="top-container">
  <div class="jumbotron jumbotron-fluid">
    <div class="mx-auto" style="width: 300px;">
      <h1 class="display-３">NEW ITEM</h1>
      <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <%= image_tag 'sandal1.jpg', class: "w-100" %>
            <div class="carousel-caption d-none d-md-block">
                <h5 class="text-dark mt-4">Speing/Summer2020</h5>
            </div>
          </div>
          <div class="carousel-item">
            <%= image_tag 'sandal2.jpg', class: "w-100" %>
            <div class="carousel-caption d-none d-md-block">
                <h5 class="text-dark">Speing/Summer2020</h5>
            </div>
          </div>
          <div class="carousel-item">
            <%= image_tag 'sandal3.jpg', class: "w-100" %>
            <div class="carousel-caption d-none d-md-block">
                <h5 class="text-dark">Speing/Summer2020</h5>
            </div>
          </div>
        </div>
      </div>
    </div>
</div>
<div class="row mx-5">
    <div class="col-3">
      <div class="list-group">
        <h4 class="mt-3">カテゴリ別に探す</h4>
        <div class="btn-group dropright">
          <button type="button" class="btn btn-warning dropdown-toggle mb-1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              WOMEN
          </button>
          <ul class="dropdown-menu">
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
                <%= f.hidden_field :category, value: "sneakers" %>
                <%= f.hidden_field :gender_type, value: "women" %>
                <%= f.submit "スニーカー", class: "bg-white text-dark border-0" %>
              <% end %>
            </li>
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
                <%= f.hidden_field :category, value: "heels" %>
                <%= f.hidden_field :gender_type, value: "women" %>
                <%= f.submit "ヒール", class: "bg-white text-dark border-0" %>
              <% end %>
            </li>
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
              <%= f.hidden_field :category, value: "sandals" %>
              <%= f.hidden_field :gender_type, value: "women" %>
              <%= f.submit "サンダル", class: "bg-white text-dark border-0" %>
            <% end %>
            </li>
          </ul>
        </div>
        <div class="btn-group dropright">
          <button type="button" class="btn btn-warning dropdown-toggle mb-1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              MEN
          </button>
          <ul class="dropdown-menu">
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
                <%= f.hidden_field :category, value: "sneakers" %>
                <%= f.hidden_field :gender_type, value: "men" %>
                <%= f.submit "スニーカー", class: "bg-white text-dark border-0" %>
              <% end %>
            </li>
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
                <%= f.hidden_field :category, value: "sandals" %>
                <%= f.hidden_field :gender_type, value: "men" %>
                <%= f.submit "サンダル", class: "bg-white text-dark border-0" %>
              <% end %>
            </li>
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
              <%= f.hidden_field :category, value: "dress_shoes" %>
              <%= f.hidden_field :gender_type, value: "men" %>
              <%= f.submit "ドレスシューズ", class: "bg-white text-dark border-0" %>
            <% end %>
            </li>
          </ul>
        </div>
        <div class="btn-group dropright">
          <button type="button" class="btn btn-warning dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              KIDS
          </button>
          <ul class="dropdown-menu">
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
                <%= f.hidden_field :category, value: "sneakers" %>
                <%= f.hidden_field :gender_type, value: "kids" %>
                <%= f.submit "スニーカー", class: "bg-white text-dark border-0" %>
              <% end %>
            </li>
            <li>
              <%= form_with scope: :search_product, url: search_product_path, method: :get do |f| %>
                <%= f.hidden_field :category, value: "sandals" %>
                <%= f.hidden_field :gender_type, value: "kids" %>
                <%= f.submit "サンダル", class: "bg-white text-dark border-0" %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </div>
    
    <div class="col-9">
      <h4 class="mt-3">アイテム一覧</h4>
      <div id="products_area"><%= render "products/products" %></div>
    </div>
  </div>
</div>


